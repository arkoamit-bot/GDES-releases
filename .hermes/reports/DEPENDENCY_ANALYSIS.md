# GDES Dependency Analysis Report

**Generated:** 2026-07-21  
**Repository:** C:\Users\User\Documents\GitHub\GDES  
**Scope:** requirements.txt, package.json, and transitive dependency analysis

---

## 1. Python Dependencies (requirements.txt)

### Active Dependencies

| Package | Specifier | Latest (Jul 2026) | Status |
|---------|-----------|-------------------|--------|
| Django | >=5.0 | 5.2.x | ✅ Current |
| djangorestframework | >=3.15 | 3.15.x | ✅ Current |
| django-jazzmin | >=3.0 | 3.0.x | ✅ Current |
| WeasyPrint | >=60 | 63.x | ⚠️ Minor behind |
| openpyxl | >=3.1 | 3.1.x | ✅ Current |
| pyreadstat | >=1.2 | 1.2.x | ✅ Current |
| pandas | >=2.0 | 2.2.x | ✅ Current |
| waitress | >=3.0 | 3.0.x | ✅ Current |
| whitenoise | >=6.6 | 6.8.x | ⚠️ Minor behind |
| xhtml2pdf | >=0.2.15 | 0.2.15 | ✅ Current |
| celery | >=5.4 | 5.5.x | ⚠️ Minor behind |
| redis | >=5.0 | 5.2.x | ⚠️ Minor behind |

### Dependency Issues

1. **No version pinning** — All dependencies use `>=` with no upper bounds. This is a **high-risk pattern** because:
   - Breaking changes in major versions can silently break the app
   - No reproducible builds without a lock file
   - Recommended: Pin to `==` or use `~=` (compatible release) with an upper bound

2. **Missing `pyproject.toml` or lock file** — No `requirements.lock`, `pip-tools` output, or `poetry.lock` exists. Builds are not reproducible.

3. **No security scanning configured** — No `safety`, `pip-audit`, or `dependabot` configuration detected.

### Commented-Out / Optional Dependencies

| Package | Status | Risk |
|---------|--------|------|
| psycopg>=3.1 | Commented out | ⚠️ SQLite-to-PostgreSQL migration path incomplete |
| gunicorn>=22.0 | Commented out | ℹ️ Desktop-only deployment (waitress used) |
| twilio>=9.0 | Commented out | ℹ️ SMS not yet implemented |
| django-csp>=3.8 | Commented out | 🔴 **Security gap** — no Content-Security-Policy headers |
| django-ratelimit>=4.1 | Commented out | 🔴 **Security gap** — no rate limiting on auth endpoints |
| pyinstaller>=6.0 | Commented out | ℹ️ Build-time only, reasonable |

### Vulnerable Dependency Patterns

| Risk | Description |
|------|-------------|
| **celery>=5.4** | Celery 5.x has known CVEs in older versions; should pin to >=5.4.1 |
| **xhtml2pdf>=0.2.15** | ReportLab (transitive) has had RCE vulnerabilities in older versions |
| **WeasyPrint>=60** | Older WeasyPrint versions had CSS injection risks |
| **pandas>=2.0** | Pickle deserialization risks if untrusted data is loaded |

## 2. JavaScript Dependencies (package.json)

The project uses Tailwind CSS via `static_src/` with minimal npm deps:

```
- tailwindcss (via static_src/)
- flowbite.min.js (133KB vendored in static/)
- chart.umd.min.js (202KB vendored in clinic/static/)
- Font Awesome (vendor/fontawesome/)
```

**Issues:**
- Vendored JS files are outdated and unversioned — no mechanism to update
- No npm audit or lock file management

## 3. System-Level Dependencies (Dockerfile)

The Dockerfile installs:
- Python 3.x base image (version not pinned)
- apt packages: `libpango-1.0-0`, `libpangocairo-1.0-0`, `libgdk-pixbuf-2.0-0`, `libffi-dev`, `libcairo2`, `gdk-pixbuf-loader` (WeasyPrint GTK deps)

**Issues:**
- Base Docker image not pinned to a specific version
- No multi-stage build to reduce attack surface

## 4. Recommendations

| Priority | Action |
|----------|--------|
| 🔴 High | Pin all dependencies to specific versions (e.g., `Django==5.2.1`) |
| 🔴 High | Generate a lock file (`pip freeze > requirements-lock.txt` or adopt `uv`) |
| 🔴 High | Enable `django-csp` and `django-ratelimit` |
| 🟡 Medium | Run `pip-audit` or `safety check` against current deps |
| 🟡 Medium | Pin Docker base image to specific version |
| 🟢 Low | Remove vendored JS; manage via npm with `npm audit` |
| 🟢 Low | Remove commented-out dependencies or document as optional extras |

---

*Report generated by automated analysis. Verify latest versions against PyPI before acting.*
